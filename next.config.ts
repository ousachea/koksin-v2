import type { NextConfig } from "next";

// Plain CSS — no Tailwind, no UI framework. Styling lives in
// styles/main.css as class-based rules.
const nextConfig: NextConfig = {};

export default nextConfig;
